// Top-level build file (minimal skeleton). Open in Android Studio and let it populate the rest.
plugins {
    id("com.android.application") version "8.1.0" apply false
    id("org.jetbrains.kotlin.android") version "1.9.10" apply false
}

// For a working project open this folder in Android Studio and follow prompts to sync and add SDKs.
